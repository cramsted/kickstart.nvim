return {
	-- TODO: going to take some work to customize right. Vanity project that I'm going to ignore for now'
	-- 'goolord/alpha-nvim',
	-- dependencies = { 'nvim-tree/nvim-web-devicons' },
	-- config = function()
	-- 	require 'alpha'.setup(require 'alpha.themes.startify'.config)
	-- end
}
